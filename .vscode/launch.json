{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Rayito's Bar — Local",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:5000",
            "webRoot": "${workspaceFolder}/public",
            "preLaunchTask": "Firebase: Serve local"
        }
    ]
}
